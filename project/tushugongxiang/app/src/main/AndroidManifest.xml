<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.tushugongxiang">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".SocketConnect"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Enrol" />
        <activity android:name=".Reader" />
        <activity android:name=".CheckBook" />
        <activity android:name=".CheckPlace" />
        <activity android:name=".Administrator" />
        <activity android:name=".Encoding" />
        <activity android:name=".Comment"></activity>
    </application>

</manifest>